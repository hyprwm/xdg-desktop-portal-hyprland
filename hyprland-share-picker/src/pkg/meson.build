exe_dir = include_directories('.',
    join_paths('.', 'notebook'),
    join_paths('.', 'region'),
    join_paths('.', 'screen'),
    join_paths('.', 'tools'),
    join_paths('.', 'window')
)

subdir('notebook')
subdir('region')
subdir('screen')
subdir('tools')
subdir('window')
